{
  "name": "wcag-figma-plugin",
  "private": true,
  "version": "0.3.1",
  "description": "Maanak — accessibility checker for Figma (WCAG 2.1/2.2 AA · GIGW 3.0 · IS 17802)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types --no-warnings 'src/**/__tests__/*.test.ts'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
